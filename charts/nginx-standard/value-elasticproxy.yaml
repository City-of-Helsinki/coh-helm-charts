nginx:
  useCase: "elastic-proxy"
# ============================================================================
# ELASTIC PROXY USE CASE CONFIGURATION (Overrides)
# ============================================================================
elasticProxy:
  enabled: false # Set to true when using elastic-proxy use case
  authSecretName: ""
  upstream:
    url: "https://elasticsearch-host1.cluster.svc:9200" # ELASTICSEARCH_URL env var